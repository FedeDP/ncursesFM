/* TODO:
 *
 * update doc
 *
 * let ncursesFM build on non-linux systems (?)
 * 
 * in device_mode:
 * Add available space for mounted fs?
 * Move available space (and other info, may be, such as mount opts) to show_stat?
 * 
 * switch to copy_file_range() function (instead of read() and write()) in paste if linux >= 4.5
 */


