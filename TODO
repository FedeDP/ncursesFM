/* TODO:
 * fm_functions doc
 * semplify code
 * search-inside_archive...try again to use same struct archive *a (global), so i'll have to add read_support_format(all) etc etc only once-> better performance
 */
