/* TODO:
 * add passphrase support to libarchive
 * check open, fopen errors
 * extend to N extracting/pasting/compressing thread
 * extend searching to one for tab; add ability to change tab even when in search mode
 */
