/* TODO:
 * fix naming -> easy
 * DOCS DOCS DOCS!!!
 * add a queued_jobs glob var to print something like "job %d of %d done", or "[job %d/%d] Pasting files..." ??
 * what happens if while we're generating md5sum, the file gets removed/renamed by other thread?
 * queue even create_dir /new_file?
 */
